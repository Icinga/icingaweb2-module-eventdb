<div class="controls">
    <?= $this->tabs ?>
</div>
<div class="content">
    <table class="name-value-table">
        <?php $event = $this->event($this->eventData); foreach ($displayColumns as $column): ?>
            <tr>
                <?= $this->columnHeader($column) ?>
                <?= $this->column($column, $event) ?>
            </tr>
        <?php endforeach ?>
    </table>
</div>

